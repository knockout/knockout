# To install Jekyll and run locally:
# 1. Have Ruby >= 1.9.3 installed (check ruby -v)
# 2. gem install bundler
# 3. bundle install
# 4. bundle exec jekyll serve

source 'https://rubygems.org'
gem 'github-pages'
