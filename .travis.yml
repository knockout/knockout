language: node_js
node_js:
- '0.10'
env:
  global:
  - CI_AUTOMATE_PROJECT="$TRAVIS_REPO_SLUG"
  - CI_AUTOMATE_BUILD="Travis build No. $TRAVIS_BUILD_NUMBER for $TRAVIS_REPO_SLUG"
script:
  - gulp test:phantomjs --once
  - sleep 10 && gulp test:phantomjs --once --jquery
  - sleep 10 && gulp test:sauce --chrome
  - sleep 10 && gulp test:sauce --firefox
  - sleep 10 && gulp test:sauce --mobile
  - sleep 10 && gulp test:sauce --opera
  - sleep 10 && gulp test:sauce --ie
  - sleep 10 && gulp test:sauce --ie-legacy --innershiv --json2
