language: node_js
node_js:
- '0.10'
env:
  global:
  - CI_AUTOMATE_PROJECT="$TRAVIS_REPO_SLUG"
  - CI_AUTOMATE_BUILD="Travis build No. $TRAVIS_BUILD_NUMBER for $TRAVIS_REPO_SLUG"
script:
  - gulp test:phantomjs --once
  - gulp test:phantomjs --once --jquery
  - gulp test:sauce --chrome
  - gulp test:sauce --firefox
  - gulp test:sauce --mobile
  - gulp test:sauce --ie
  - gulp test:sauce --ie-legacy --innershiv --json2
